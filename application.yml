spring:
  profiles:
    active:
      - dev

---
spring:
  profiles: dev
  application:
    name: microservicecloud-config-ax-dev

---
spring:
  profiles: test
  application:
    name: microservicecloud-config-ax-test